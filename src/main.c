#include <stdio.h>
#include <stdlib.h>
#include "mapint.h"
#include "connect.h"

int main(void) {
	
	startServer(6666);
	
//	Mapint* mapint = mapint_create(1024);
//	mapint_add(mapint, "aa", 23);
//	mapint_add(mapint, "bb", 48);
//	mapint_add(mapint, "cc", 5);
//	
//	MapintNode** nodes = mapint_getNodes(mapint);
//	for(int i = 0; ; i++) {
//		MapintNode* node = nodes[i];
//		if(node == NULL) {
//			break;
//		}
//		printf("%s :: %d\n", node->key, node->value);
//	}
//	
//	mapint_free(mapint);
	
	puts("ook");
	return 0;
}
